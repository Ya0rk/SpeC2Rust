而且我觉得在代码生成时 可以边生成边cargo build和报错修复。
让agent理解了要生成的项目，先自底向上生成一条单路径（我们称该路径为主树干调用路径，有的项目可能有多条主树干路径，随机选一条就行，其他的后续再迭代完成），该路径中每个节点就是一个函数，如果该函数a需要调用路径之外的其他函数b（这种类型的函数我们称之为树枝函数），可以将该树枝函数设置为unimplement，确保编译通过，然后在此基础上不断扩充树干的树枝，让项目慢慢扩充生长，知道覆盖完spec文档，最后测试修复

让这棵树不断“生长”

类似懒处理的思想，大模型现在最需要的就是上下文，让它的注意力集中于一小块能提高准确度

要规划出最小可编译集，然后逐步扩展